[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "clement-studio-skills-hub"
version = "0.1.0"
description = "Registry, validation and lifecycle tooling for CLEMENT STUDIO skills"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["loaders", "validators"]